<%# Workspace layout — extends engine_design_system/application with nav bar %>
<% content_for :tab_navigation do %>
  <%= ds_nav_bar(engine_name: @engine_name, bindables: @bindables || []) if @engine_name %>
<% end %>

<%= render layout: "engine_design_system/application" do %>
  <%= yield %>
<% end %>
